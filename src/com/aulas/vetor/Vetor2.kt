package com.aulas.vetor

class Vetor2 {

   /* private lateinit var elementos: Array<String>
    private var tamanho = 0

    fun Vetor(capacidade: Int) {
        elementos = arrayOf(capacidade).toString()
        tamanho = 0
    }

    fun adiciona(elemento: String): Boolean {
        if (tamanho < this.elementos.size) {
            elementos.get(tamanho) = elemento // add elemento no nosso vetor
            tamanho++
            return true
        }
        return false
    }*/
}
